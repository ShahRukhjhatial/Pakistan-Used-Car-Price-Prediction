# -*- coding: utf-8 -*-
"""Mercedes.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1bd5yYLR9y0m0mGSy-nR6_aL_1HpCHjDb
"""

import keras
import numpy as np
import pandas as pd
import io

from google.colab import files 
  
  
uploaded = files.upload()

mercedes = pd.read_csv(io.BytesIO(uploaded['Mercedes.csv']))

namedum = pd.get_dummies(mercedes.Name)
mercedes = mercedes.join(namedum)
colourdum = pd.get_dummies(mercedes.Color,prefix="is")
mercedes=mercedes.join(colourdum)
enginedum = pd.get_dummies(mercedes['Engine Type'])
mercedes=mercedes.join(enginedum)
Bodydum = pd.get_dummies(mercedes['Body Type'])
mercedes=mercedes.join(Bodydum)
mercedes.drop(['Name', 'Color', 'Engine Type', 'Body Type'], axis=1, inplace=True)

mercedes['Anti lock Braking System']=mercedes['Anti lock Braking System'].fillna(0)

mercedes.shape

Y = mercedes.Price
mercedes.drop(['Price'], axis=1, inplace=True)
X=mercedes

from sklearn.model_selection import train_test_split
X_train, X_test, Y_train, Y_test = train_test_split(X, Y, test_size=0.25,random_state=0)

model = keras.models.Sequential()

model.add(keras.layers.Dense(128, activation='relu', input_shape=(81,)))
model.add(keras.layers.Dense(64, activation='relu'))
model.add(keras.layers.Dense(64, activation='relu'))
model.add(keras.layers.Dense(32, activation='relu'))
model.add(keras.layers.Dense(16, activation='relu'))
model.add(keras.layers.Dense(1, activation='linear'))

keras.optimizers.Adam(lr=0.0001, beta_1=0.9, beta_2=0.999, amsgrad=False)
model.compile(loss='mean_squared_error', optimizer='adam', metrics=['mean_absolute_percentage_error'])

model.fit(X, Y, epochs=10000,batch_size=50, callbacks=[keras.callbacks.EarlyStopping(patience=3)])

pred=model.predict(X_test, batch_size=1)

actual=pd.DataFrame(Y_test) 
pred=pd.DataFrame(pred)

actual.shape

actual

pred.columns=['Pred']

a=[]
for i in range (123):
  a.append(i)
actual.index=a

actual

mercedes=pd.concat([pred, actual], axis=1)

mercedes

from sklearn import metrics
import sklearn.metrics as sm
print("Mean absolute error =", round(sm.mean_absolute_error(mercedes.Pred, mercedes.Price))) 
print("Mean squared error =", round(sm.mean_squared_error(mercedes.Pred, mercedes.Price)))
print("Median absolute error =", round(sm.median_absolute_error(mercedes.Pred, mercedes.Price))) 
print("Explain variance score =", (sm.explained_variance_score(mercedes.Pred, mercedes.Price)))
print("R2 score =", (sm.r2_score(mercedes.Pred, mercedes.Price)))

import matplotlib.pyplot as plt
plt.figure(figsize=(100,200))
fig, ax = plt.subplots()
ax.plot(mercedes.index,mercedes['Pred'],color='blue',marker='o')
ax.plot(mercedes.index,mercedes['Price'], color='orange',marker='*')

pred=0
act=0
for i in range (123):
  pred=pred+mercedes.Pred[i]
  act=act+mercedes.Price[i]

import matplotlib.pyplot as plt
fig = plt.figure()
ax = fig.add_axes([0,0,1,1])
langs = ['Actual','Predicted']
students = [act,pred]
ax.bar(langs[0],students[0],color='black')
ax.bar(langs[1],students[1],color='blue')
plt.show()

import numpy as np
import matplotlib.pyplot as plt
import random
a=random.randrange(123)
b=random.randrange(123)
c=random.randrange(123)
d=random.randrange(123)
e=random.randrange(123)
f=random.randrange(123)
g=random.randrange(123)
h=random.randrange(123)
i=random.randrange(123)
j=random.randrange(123)
data= [[mercedes.Pred[a],mercedes.Pred[b],mercedes.Pred[c],mercedes.Pred[d],mercedes.Pred[e],mercedes.Pred[f],mercedes.Pred[g],mercedes.Pred[h],mercedes.Pred[i],mercedes.Pred[j]],
     [mercedes.Price[a],mercedes.Price[b],mercedes.Price[c],mercedes.Price[d],mercedes.Price[e],mercedes.Price[f],mercedes.Price[g],mercedes.Price[h],mercedes.Price[i],mercedes.Price[j]]]
fig = plt.figure()
ax = fig.add_axes([0,0,1,1])
X=np.arange(10)
ax.bar(X + 0.00, data[0], color = 'b', width = 0.25,)
ax.bar(X + 0.25, data[1], color = 'black', width = 0.25,)
ax.legend(labels=['Predicted', 'Actual'])

